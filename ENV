# this is the address of my ZeroTier bridge, so it is secure access
NGINX_IPV4_ADDRESS=192.168.1.25

# this is the /etc/hosts entry I use locally to access ZeroTier bridge
NGINX_SERVER_NAME=webappfast.chartewalk.com

# this ensures we only access it over HTTPS, to reduce confusion
NGINX_HTTPS_ONLY=1

# enable auto restart on deploy
AUTO_RESTART=1
